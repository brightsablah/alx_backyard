#include "main.h"

/**
* main - Entry point
* Description:
*
* Return: Always 0 (Success)
*/

void reverse_array(int *a, int n)
{
	int i;

	i = n;
	while(i < n)
	{
		if (i != 0)
		{
			_putchar(',');
			_putchar(' ');
		}
	}

return (0);

}
